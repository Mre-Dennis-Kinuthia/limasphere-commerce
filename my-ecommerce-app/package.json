{
  "name": "my-ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "build": "cd client && yarn build && cd ../server && yarn build",
    "start": "cd server && yarn start",
    "lint": "cd client && yarn lint",
    "lint:fix": "cd client && yarn lint:fix"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^6.4.0"
  }
}